package com.curewell.model;

public enum Category { // defined as enum because in our conception we are not going to give the possibility for anyone to add,
                        // modify or delete the categories, so Category will be always the same
    Cat1,
    Cat2,
    Cat3
}
